<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1>Eat-Da-Burger!</h1>
        <p>Welcome to our new restaurant! Add burgers you'd like to try to your list and keep track of them. Devour the best ones first, and if you pick, you have to go again!</p>
    </div>
</div>

<div class="card">
<div class="container">
    <div class="row">

        <div class="col-sm-4">

            <h2>Burgers devoured:</h2>

            <ol>
                {{#each burgers}} {{#unless this.devoured}}
                <li>
                    {{this.burger_name}}

                    <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                        <input type="hidden" name="devoured" value="1">
                        <button class="btn" type="submit">PUKE!</button>
                    </form>
                </li>
                {{/unless}} {{/each}}
            </ol>
        </div>

        <div class="col-sm-4">
            <h2>Burgers to try:</h2>
            <ol>
                {{#each burgers}} {{#if this.devoured}}
                <li>
                    {{this.burger_name}}

                    <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                        <input type="hidden" name="devoured" value="0">
                        <button class="btn" type="submit">DEVOUR!</button>
                    </form>
                </li>
                {{/if}} {{/each}}
            </ol>
        </div>

        <div class="col-sm-4">
            <h2>Add a burger:</h2>
            <form class="create-update-form" action="burgers/create" method="POST">

                <div class="form-group">
                    <label for="name">Name:</label>
                    <input type="text" id="bu" name="burger_name">
                </div>

                <div class="form-group">
                    <label for="devoured">Devoured?</label>
                    <br>
                    <input type="radio" name="devoured" value="0"> Devoured
                    <br>
                    <input type="radio" name="devoured" value="1"> Not Devoured
                </div>

                <button type="submit" class="btn">Add Burger</button>
            </form>
        </div>

    </div>
</div>

</div>